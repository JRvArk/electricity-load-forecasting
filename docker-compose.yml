# Phase 4 — service + mlflow. Phase 5 scheduling is systemd timers on the host, not a worker
# service here (decided 2026-09-07; Prefect dropped).
services:
  mlflow:
    image: ghcr.io/mlflow/mlflow:latest
    command: mlflow server --host 0.0.0.0 --port 5000 --backend-store-uri /mlruns
    ports:
      - "5000:5000"
    volumes:
      - ./mlruns:/mlruns

  api:
    build: .
    depends_on:
      - mlflow
    environment:
      # point the app at the mlflow service instead of the local file store
      MLFLOW_TRACKING_URI: http://mlflow:5000
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./mlruns:/app/mlruns
